# Project Memory — Index

One line per durable, reusable, non-obvious project pattern or anti-pattern.

**Never write secrets, credentials, or PII here — this file is committed to git.**

<!-- index entries go below -->
- Pi dependency correction must not require integrating a blocked consumer first. Preserve the immutable admission base; host merges carry a separate, receipt-bound scope baseline and invalidate HEAD-bound reviews.
