# Project Memory — Index

One line per durable, reusable, non-obvious project pattern or anti-pattern. The full prose lives in
a topic file next to this index (`<slug>.md`); link it as `- [Title](file.md) — hook`.

This index loads at the start of every session (first 200 lines / 25KB). Topic files load on demand.
The `shipper` commits this directory back so cloud routines accumulate knowledge across runs.

**Never write secrets, credentials, or PII here — this directory is committed to git.**

<!-- index entries go below, e.g.:
- [Auth session lives in KV](auth-session.md) — reuse `getSession()` before adding a new store
-->

- [Dispatch-hand contract](dispatch-hand-contract.md) — truth = git diff + `captured:true` flag, fail-closed; redact-first then truncate; per-dispatch allowedWrites; frozen manifest excludes executor writes; commit before re-freezing/re-spawning on the same file (reconciliation eats uncommitted work); mark.mjs marker stdout must stay visible + fire as standalone calls after the freeze-commit; harness-internal infra writes (version-check cache) excluded from the scope/gitignored-escape sweep by EXACT match only, never prefix
- [Model strategy split](model-strategy-split.md) — `hand_tiers` (Ollama write-roles) vs eye roles (always Claude); shape detection; CLAUDE_ALIASES; ALLOWED_MS_KEYS; legacy `tiers` removed (rejected by validation); prefer high-tier Ollama models (kimi-k2.7-code) over medium (glm-5.2) for surgical/composition-root fixes — observed flakiness
- [Harness repo constraints](harness-repo-constraints.md) — `package.json` holds `version` (no root `VERSION` file); no tsconfig/eslint, only `node --test`; sandbox blocks .env.* reads in tests (run with sandbox off); dual-mirror edit rule is STALE (harness removed from global ~/.claude/ 2026-06-28 — edit core/ only, no phantom second copy to hunt for; .claude/ itself is still gitignored source-side — existsSync-guard drift tests); import.meta.url path resolution in test files; prompt-rule locked tests on an agent .md must assert co-occurrence within the rule's own section window, never a whole-body token search (vacuous-pass / silent-erosion risk); `extractSection` matches HEADINGS only — returns "" (vacuous-pass risk) on a bullet target; absence checks on multi-token code literals must pin the single distinctive token, scoped to the region — line-reflow defeats the contiguous literal
- [Independent PR-review phase](independent-pr-review-phase.md) — **[RETIRED ENGINE]** the whole cron-review composition (cron-review.mjs/run-cron-review.mjs/spawn-review-session.mjs) was deleted in #807; kept for the traps that outlive it — NODE (never the LLM session) derives the canonical verdict, erase the derivation INPUT not just the output, never coerce a fail-closed sentinel to `[]`, scrub hand tokens from a child fed an untrusted patch, a gate predicate must stay SYNCHRONOUS
- [Codex cross-family gitignored evidence](codex-cross-family-gitignored-evidence.md) — Codex's own file-search under `codex exec --sandbox read-only` appears to respect `.gitignore`, so it reports a real evidence file under a gitignored path (e.g. `.claude/plans/`) as "does not exist"; inline gitignored-path evidence into the task JSON instead of citing only the path, or verify a Codex "file missing" report directly before trusting it as a refutation
- [PostToolUse[Agent] agent_id contract](posttooluse-agent-id-contract.md) — main-loop dispatch OMITS `agent_id`, nested dispatch INCLUDES it; guard "main-loop only" with `hasOwnProperty` presence-check, never truthiness
- [Rule reach requires agent contract](rule-reach-requires-agent-contract.md) — a convention added only to `core/rules/*.md` is decorative for a Read/Write-only subagent (no `Skill` tool, no rule-injection hook) — inline it into that actor's own `core/agents/<name>.md` (checked against `vendor-core.mjs`'s `FRAMEWORK_OWNED`) to actually reach it
- [Retention-sweep fail-closed guards](vps-retention-sweep-fail-closed-guards.md) — **[RETIRED ENGINE]** paid for in the deleted VPS sweep, applies to ANY destructive path: destructuring defaults don't catch `null`/`""` (`Number(null) === 0`); pass resolvedOpts not raw opts to helpers; a normalized `gh` seam can't distinguish outage from empty (use the raw spawn seam on a destructive path); an unreadable events log must drop the candidate, not read as empty; truncate a log BEFORE resetting a positional-index field derived from it
- [Writer invariant: chatId/threadId](vps-writer-invariant-chatid-threadid.md) — every writer of `meta.threadId` (today only `core/shared/lib/obs-outbox.mjs`; the second writer, the Telegram drain, was deleted in #834 — `docs/vps-retirement.md`) must also stamp `meta.chatId` from the same API result (never `config.notify.chatId`) — `message_thread_id` is per-chat, not global; a migrated Telegram group otherwise defeats any cross-tenant gate
- [Telegram notifier (best-effort one-way)](vps-notify-telegram.md) — the module was RETIRED and deleted in #834 (moved from `core/vps/` in #807, then removed with no production caller — see `docs/vps-retirement.md`); best-effort/fail-open notifier (fire-and-forget + drain-before-exit); token only in ~/.claude/.dev.vars, log redacted to {op,type,project,status}; producer/consumer event-shape (type/casing/field-names) is an implicit cross-task contract, recurred a 4th time (#178) — per-task frozen tests assert SENT not RENDERED, only a feature-wide enumeration catches a silent divergence; the vocabulary itself survives in `core/shared/lib/obs-event-types.mjs`
- [Test the real composition-root binding](test-real-composition-root-binding.md) — a frozen test injecting a fake seam can pass green while the real wiring (bound in the composition root, not the pure-logic module) throws or reads the wrong data; require a paired POSITIVE control on the real fixture for any negative locked test on a destructive path
- [Reaper multi-repo gh scoping](reaper-multi-repo-gh-scoping.md) — **[RETIRED ENGINE]** the reaper was deleted in #807; the rule survives: two DISTINCT gh scoping seams (a normalized `scopedGh` vs a raw `spawnSync` with different fail-mode contracts) must each be asserted independently — a test exercising one proves nothing about the other, and a seam that cannot distinguish "empty" from "could not ask" may never gate a destructive action
- [Secret-shaped fixture fragmentation](secret-shaped-fixture-fragmentation.md) — `scan-secrets-in-tree` locked-2 scans the whole tracked tree and self-flags a literal secret-shaped fixture (`ghp_`/`sk-proj-`/`github_pat_`/JWT); assemble from ≤16-char concatenated fragments referenced by the SAME variable in both input and assertion — byte-identical runtime value, scanner never sees the full string on one line
- [cron-a-exit reason capture](cron-a-exit-reason-capture.md) — **[RETIRED ENGINE]** module deleted in #807; raw-log unlink (worse-than-summary because unscrubbed) must be in a `finally` wrapping every fallible step after the write, not sequential; a value-based scrubber has a NAMED coverage boundary (`KEY|TOKEN|SECRET|PASSWORD|AUTH` + shape patterns) — `DATABASE_URL`/`REDIS_URL`/DSN were outside it, widen from the start in any new redactor
- [Outbox event ts-field comparison](outbox-event-ts-field-comparison.md) — never `assert.deepEqual` a whole outbox event; `appendEvent` always stamps `ts` — compare fields individually
- [orchestrating-delivery SKILL.md byte budget](orchestrating-delivery-skillmd-byte-budget.md) — near-zero headroom vs `orchestrator-economy.test.mjs`'s hard budget gate; any edit to that SKILL.md must run that test explicitly
- [Checkpoint position before branching](checkpoint-position-before-branching.md) — a verdict-triggered checkpoint marker must sit BEFORE any stop/proceed branch in an LLM-read SKILL.md, never after — textual order is load-bearing
