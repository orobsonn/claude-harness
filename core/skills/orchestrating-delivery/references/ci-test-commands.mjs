/**
 * @description SCAFFOLD STUB — un-vacuums the frozen gate for task-1 (ci-test-commands).
 * The executor replaces every throwing body with the real implementation until the frozen
 * locked test (ci-test-commands.test.mjs) goes green. See the API contract in
 * .claude/plans/gate-all-ci-configs/run/shared_context.md.
 */

/** @description STUB — see freeze scaffold note. */
export function enumerateCiTestCommands() {
  throw new Error("not implemented");
}

/** @description STUB — see freeze scaffold note. */
export function runCiTestCommands() {
  throw new Error("not implemented");
}

/** @description STUB — see freeze scaffold note. */
export function ciSuiteExitCode() {
  throw new Error("not implemented");
}
