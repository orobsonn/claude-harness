/**
 * @description Fresh-verdict source for the independent PR-review phase. Scaffold stub — replaced by
 * the executor with the real implementation.
 */
export function getFreshVerdict(pr, sha, stateDir) {
  throw new Error("not implemented");
}
