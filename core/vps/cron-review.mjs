/**
 * @description Node layer of the independent PR-review phase: lists open PRs, origin-gates them,
 * checks pr:sha idempotency, spawns the review session, reads the engine-controlled verdict artifact,
 * enforces the merge conjunction at the boundary, and drives per-cycle reconciliation. Scaffold stub —
 * replaced by the executor.
 */
export function cronReview(opts) {
  throw new Error("not implemented");
}
