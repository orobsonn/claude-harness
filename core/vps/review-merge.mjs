/**
 * @description Deterministic merge + finalize + MERGED-state reconciliation for the independent
 * PR-review phase. Scaffold stub — replaced by the executor with the real implementation.
 */
export function mergeAndFinalize(pr, sha, opts) {
  throw new Error("not implemented");
}
export function reconcile(opts) {
  throw new Error("not implemented");
}
