/** @description Scaffold stub — real implementation delivered by the executor. Exports the exact
 * named symbols the frozen test imports so the test collects (un-vacuous gate) while staying RED. */

export function hasEnoughFreeMemory() {
  throw new Error("not implemented");
}

export function defaultFreeMem() {
  throw new Error("not implemented");
}

export const DEFAULT_MEM_GUARD_BYTES = 0;
