/**
 * @description Reject / chain-depth routing for the independent PR-review phase. Scaffold stub —
 * replaced by the executor with the real implementation.
 */
export function routeReject(pr, sha, opts) {
  throw new Error("not implemented");
}
