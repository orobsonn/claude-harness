# Claude Code local
.claude/settings.local.json
.claude/plans/
.claude/plans/current.md
.claude/plans/*.local.md

# Secrets — Ollama auth token (dispatch-hand) and any env file, never commit
.dev.vars
.dev.vars.*
.env
.env.*
# ...but the placeholder example (no real values) IS tracked so contributors know the keys
!.dev.vars.example

# Run buffers / session dirs — ephemeral harness artifacts, never commit
/findings.md
/projects/

# OS
.DS_Store
*.log

# Material de trabalho com paths locais / dados pessoais — não distribuir
docs/raw/
